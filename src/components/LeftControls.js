import React from "react"
import { Controls } from "./SynthLayout"
import Envelope from "./Envelope/Envelope"

export default () => {
  return (
    <Controls>
      <Envelope />
    </Controls>
  )
}
